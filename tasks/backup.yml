---
###############################################################################
# Backup
###############################################################################
# Only backup SQLite databases and configuration file.

- name: 'Backup | fact | parse location'
  ansible.builtin.set_fact:
    sonarr__archive: '{{ "/tmp/" ~ sonarr__srv._start ~ "_config.tar.xz" }}'

- name: 'Backup | warning | ⚠ Metadata Enabled ⚠'
  when: sonarr__cfg._backup_metadata
  ansible.builtin.debug:
    msg: |

      ⚠ Metadata Enabled ⚠

      /var/opt/sonarr/MediaCover may be Multi-GB. A backup effectively doubles
      the space used during the process and may fail if low disk space.

      Transfer may be very long.


- name: 'Backup | filesystem | set backup location'
  when:
    sonarr__cfg._backup_d | length > 0 and sonarr__cfg._backup_d != '/tmp'
  delegate_to: 'localhost'
  run_once: true
  ansible.builtin.file:
    path: '{{ sonarr__cfg._backup_d }}'
    mode: '0750'
    state: directory

- name: '{{  "Backup | cmd | create " ~ sonarr__archive }}'
  ansible.builtin.shell: '{{
      "/usr/bin/tar " ~
      ("--exclude=MediaCover" if not sonarr__cfg._backup_metadata else "") ~
      " -cvJf " ~ sonarr__archive ~ " ."
    }}'
  args:
    executable: '/bin/bash'
    chdir: '{{ sonarr___app._var_d }}'
  changed_when: true

- name:
    '{{ "Backup | fetch | " ~ sonarr__archive }}'
  ansible.posix.synchronize:
    mode: pull
    src: '{{ sonarr__archive }}'
    dest: '{{ sonarr__cfg._backup_d ~ "/" }}'

- name: 'Backup | filesystem | cleanup'
  ansible.builtin.file:
    path: '{{ "/tmp/" ~ sonarr__srv._start ~ "_config.tar.xz" }}'
    state: 'absent'

- name: 'Backup | start'
  ansible.builtin.import_tasks: 'start.yml'

- name: 'Backup | success ✔'
  ansible.builtin.meta: 'end_host'
