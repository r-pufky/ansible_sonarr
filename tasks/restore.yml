---
###############################################################################
# Restore
###############################################################################
# Find the latest set of backups and copy to remote. Restart services and
# validate they are running from role.
#
# Use synchronize instead of unarchive as metadata may be multi-GB.

- name: 'Restore | filesystem | enumerate all backups'
  delegate_to: 'localhost'
  ansible.builtin.find:
    paths: '{{ sonarr__cfg._backup_dir }}'
    patterns: '*.tar.xz'
    file_type: 'file'
    recurse: false
  register: sonarr__archive

# Grab datestamp from expected 'YYYY-MM-DDTHHMMSS_{FILE}' format.
- name: 'Restore | fact | parse latest backup'
  when: sonarr__archive.files | length > 0
  ansible.builtin.set_fact:
    sonarr__restore_archive: '{{

        (
          sonarr__archive.files |
          sort(attribute="mtime", reverse=true) |
          first
        ).path
      }}'

- name: '{{ "Restore | copy | " ~ sonarr__restore_archive }}'
  ansible.posix.synchronize:
    src: '{{ sonarr__restore_archive }}'
    dest: '{{ "/tmp/" ~ sonarr__restore_archive | basename }}'

- name:
    '{{ "Restore | filesystem | /tmp/" ~ sonarr__restore_archive | basename }}'
  ansible.builtin.command: '{{
      "/usr/bin/tar -xJpf " ~
      "/tmp/" ~ sonarr__restore_archive | basename ~
      " -C " ~ sonarr__cfg._var_dir ~ " --overwrite"
    }}'
  changed_when: true

- name: 'Restore | filesystem | cleanup'
  ansible.builtin.file:
    path: '{{ "/tmp/" ~ sonarr__restore_archive | basename }}'
    state: 'absent'

- name: 'Restore | start'
  ansible.builtin.import_tasks: 'start.yml'

- name: 'Restore | success ✔'
  ansible.builtin.meta: 'end_host'
